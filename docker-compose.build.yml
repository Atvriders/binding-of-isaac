# Optional override for building from source instead of pulling the published image:
#
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
#
services:
  isaac:
    build:
      context: .
    pull_policy: build
